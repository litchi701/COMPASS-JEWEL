# 战略层级切换器功能说明

## 📋 功能概述

已成功实现战略层级切换器和热点列表功能，用户可以：
1. 选择市场（日本、韩国、东南亚、美国）
2. 切换战略层级（Tier-1/Tier-2/Tier-3）
3. 查看该市场该层级下的留存热点列表
4. 点击热点后，Dashboard 主体部分会动态显示该热点的完整战略分析

---

## 🎯 实现的功能

### 1. 扩展的 Market Store
**文件**: `frontend/src/stores/market.js`

新增状态：
- `currentTier`: 当前选中的层级
- `selectedTrend`: 当前选中的热点
- `tiers`: 层级列表配置

新增方法：
- `setTier(tierCode)`: 切换层级
- `selectTrend(trend)`: 选择热点
- `clearSelectedTrend()`: 清空选中的热点

新增 Getters：
- `currentMarketName`: 当前市场名称
- `currentTierName`: 当前层级名称

### 2. 层级切换器组件
**文件**: `frontend/src/components/layout/TierSelector.vue`

功能：
- 显示三个战略层级选项
- 点击切换层级
- 高亮显示当前选中的层级
- 显示层级描述信息

### 3. 热点列表组件
**文件**: `frontend/src/components/layout/TrendList.vue`

功能：
- 根据市场和层级自动加载热点列表
- 显示热点的关键信息（评分、转化率、声量等）
- 点击热点后更新全局选中状态
- 高亮显示当前选中的热点
- 支持加载状态和错误处理

### 4. 更新的 MarketSelector
**文件**: `frontend/src/components/layout/MarketSelector.vue`

集成了：
- 原有的市场选择器
- 新的层级切换器
- 新的热点列表

### 5. 动态 Dashboard
**文件**: `frontend/src/views/Dashboard.vue`

根据选中的热点动态显示：
- 热点名称
- 热点分类
- 热度评分
- 穿戴转化率
- 社交声量
- 关键洞察
- 建议行动

---

## 🎨 UI 布局

```
┌─────────────────────────────────────────────────────────┐
│                        Header                            │
├──────────────┬──────────────────────────────────────────┤
│              │                                           │
│  市场选择器   │                                           │
│  🇯🇵 日本     │                                           │
│  🇰🇷 韩国     │         Dashboard 主体内容                │
│  🌏 东南亚    │      （根据选中的热点动态显示）             │
│  🇺🇸 美国     │                                           │
│──────────────│                                           │
│  层级切换器   │                                           │
│  Tier-1     │                                           │
│  Tier-2     │                                           │
│  Tier-3     │                                           │
│──────────────│                                           │
│  热点列表     │                                           │
│  ● CHIIKAWA │                                           │
│  ● 梦幻系珍珠 │                                           │
│  ● 推し活     │                                           │
│              │                                           │
└──────────────┴──────────────────────────────────────────┘
```

---

## 🔄 交互流程

### 流程 1: 切换市场
1. 用户点击市场选择器中的某个市场（如"美国市场"）
2. `marketStore.setMarket('US')` 被调用
3. 选中的热点被清空
4. 热点列表自动重新加载美国市场当前层级的热点
5. Dashboard 显示默认热点或提示选择热点

### 流程 2: 切换层级
1. 用户点击层级切换器中的某个层级（如"Tier-2"）
2. `marketStore.setTier('Tier-2')` 被调用
3. 选中的热点被清空
4. 热点列表自动重新加载当前市场 Tier-2 的热点
5. Dashboard 显示默认热点或提示选择热点

### 流程 3: 选择热点
1. 用户点击热点列表中的某个热点
2. `marketStore.selectTrend(trend)` 被调用
3. 该热点被设置为全局选中状态
4. Dashboard 自动更新显示该热点的详细信息：
   - 热点名称
   - 分类
   - 评分
   - 转化率
   - 社交声量
   - 关键洞察
   - 建议行动

---

## 📊 数据流

```
用户操作
   ↓
Market Store (状态管理)
   ↓
TrendList 组件监听变化
   ↓
调用 API: getTrendsByTier(market, tier)
   ↓
后端返回热点列表
   ↓
显示在侧边栏
   ↓
用户点击某个热点
   ↓
更新 selectedTrend
   ↓
Dashboard 监听 selectedTrend 变化
   ↓
动态更新显示内容
```

---

## 🧪 测试步骤

### 测试 1: 基础功能
1. 启动前端和后端
2. 打开浏览器访问 http://localhost:3002
3. 观察左侧边栏是否显示：
   - 市场选择器
   - 层级切换器
   - 热点列表

### 测试 2: 切换市场
1. 点击"美国市场"
2. 观察热点列表是否更新为美国市场的热点
3. 观察 Dashboard 顶部是否显示"美国市场"

### 测试 3: 切换层级
1. 点击"Tier-2"
2. 观察热点列表是否更新为 Tier-2 的热点
3. 观察热点数量是否变化

### 测试 4: 选择热点
1. 点击热点列表中的某个热点
2. 观察该热点是否高亮显示
3. 观察 Dashboard 主体内容是否更新：
   - "当前分析热点" 显示该热点名称
   - 三个指标卡片显示该热点的数据
   - 简报部分显示该热点的洞察和建议

### 测试 5: 组合操作
1. 选择"日本市场" + "Tier-1"
2. 点击"#CHIIKAWA 错综复杂与日常黄金金饰"
3. 观察 Dashboard 显示 CHIIKAWA 的详细信息
4. 切换到"Tier-2"
5. 观察热点列表更新，选中状态被清空
6. 点击新的热点
7. 观察 Dashboard 更新为新热点的信息

---

## 🎨 样式特点

### 层级切换器
- 金色标题 (#d4af37)
- 深色背景 (#0a0a0a)
- 悬停效果：边框变金色
- 选中状态：金色边框 + 半透明金色背景

### 热点列表
- 卡片式布局
- 显示关键指标（评分、转化率、声量）
- 风险等级徽章（低/中/高）
- 悬停效果：向右平移 + 金色边框
- 选中状态：金色边框 + 发光效果

### Dashboard 动态内容
- 顶部显示当前热点名称和市场/层级信息
- 三个指标卡片动态显示热点数据
- 简报部分显示热点的洞察和建议

---

## 📝 代码示例

### 在其他组件中访问选中的热点

```vue
<script setup>
import { storeToRefs } from 'pinia'
import { useMarketStore } from '@/stores/market'

const marketStore = useMarketStore()
const { selectedTrend, currentMarket, currentTier } = storeToRefs(marketStore)

// 监听选中的热点变化
watch(selectedTrend, (newTrend) => {
  if (newTrend) {
    console.log('选中了热点:', newTrend.name)
    console.log('热点ID:', newTrend.id)
    console.log('评分:', newTrend.current_score)
  }
})
</script>
```

### 手动选择热点

```javascript
import { useMarketStore } from '@/stores/market'

const marketStore = useMarketStore()

// 选择热点
marketStore.selectTrend({
  id: 'jp-tier1-001',
  name: '#CHIIKAWA 错综复杂与日常黄金金饰',
  // ... 其他字段
})

// 清空选中
marketStore.clearSelectedTrend()
```

---

## 🔧 配置说明

### 层级配置
在 `stores/market.js` 中配置：

```javascript
tiers: [
  { 
    code: 'Tier-1', 
    name: '长线资产', 
    description: '14-30天观察期，高确定性' 
  },
  { 
    code: 'Tier-2', 
    name: '稳健验证', 
    description: '7-14天观察期，中等潜力' 
  },
  { 
    code: 'Tier-3', 
    name: '初期观察', 
    description: '1-7天观察期，待验证' 
  }
]
```

---

## ⚠️ 注意事项

1. **数据加载**
   - 切换市场或层级时会自动加载数据
   - 加载过程中显示 loading 状态
   - 加载失败会显示错误提示

2. **选中状态**
   - 切换市场或层级时会清空选中的热点
   - 这是为了避免显示不匹配的数据

3. **默认显示**
   - 如果没有选中热点，Dashboard 会显示默认数据
   - 默认数据是 CHIIKAWA 热点的信息

4. **响应式**
   - 侧边栏宽度固定为 280-320px
   - 热点列表支持滚动
   - 适配不同屏幕尺寸

---

## 🚀 下一步优化建议

1. **搜索功能**: 在热点列表中添加搜索框
2. **排序功能**: 支持按评分、转化率等排序
3. **筛选功能**: 支持按风险等级、状态筛选
4. **收藏功能**: 支持收藏常用热点
5. **对比功能**: 支持选择多个热点进行对比
6. **历史记录**: 记录用户查看过的热点

---

## 📞 使用帮助

### 如何重启前端查看效果

在前端终端按 `Ctrl+C` 停止服务，然后：

```bash
npm run dev
```

访问 http://localhost:3002 查看效果。

### 如果看不到新功能

1. 清除浏览器缓存
2. 硬刷新页面（Cmd+Shift+R 或 Ctrl+Shift+R）
3. 检查浏览器控制台是否有错误

---

**状态**: ✅ 已完成，可以投入使用

**创建时间**: 2026-05-24
