# 🚀 COMPASS JEWEL 启动指南

## ✅ 前后端连接问题已修复

### 修复内容
1. ✅ 前端 API baseURL 已配置
2. ✅ 前端环境变量文件已创建
3. ✅ 后端数据库配置已更新注释

---

## 📋 启动前准备

### 1. 数据库配置（可选）

**注意**：项目可以在没有数据库的情况下运行，但功能会受限：
- ✅ **可用功能**：实时信息流、数据统计、Agent 3 查询（使用内存数据）
- ❌ **不可用功能**：简报历史、对话历史持久化

#### 如果要使用完整功能，需要安装 MySQL：

```bash
# macOS 安装 MySQL
brew install mysql

# 启动 MySQL 服务
brew services start mysql

# 设置 root 密码（首次安装）
mysql_secure_installation

# 初始化数据库
mysql -u root -p < backend/database/init_db.sql
```

然后更新 `backend/.env` 中的数据库密码：
```
DATABASE_URL=mysql+pymysql://root:你的密码@localhost:3306/compass_jewel
```

---

## 🏃 启动步骤

### 第一步：启动后端

```bash
cd backend

# 激活虚拟环境（如果已创建）
source venv/bin/activate

# 或者创建新的虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 启动后端服务
python main.py
```

后端将运行在 `http://localhost:8000`

**验证后端是否启动成功**：
- 访问 http://localhost:8000 应该看到 `{"message": "COMPASS JEWEL API is running"}`
- 访问 http://localhost:8000/docs 查看 API 文档

### 第二步：启动前端

**打开新的终端窗口**：

```bash
cd frontend

# 安装依赖（首次运行）
npm install

# 启动开发服务器
npm run dev
```

前端将运行在 `http://localhost:5173`（Vite 默认端口）

---

## 🧪 测试连接

### 1. 测试后端 API

```bash
# 测试实时信息流（不需要数据库）
curl http://localhost:8000/api/feed/JP

# 测试数据统计
curl http://localhost:8000/api/data/stats
```

### 2. 测试前端

打开浏览器访问 `http://localhost:5173`，你应该能看到：
- ✅ Dashboard 页面正常显示
- ✅ 左侧市场选择器可以切换
- ✅ 实时信息流显示数据
- ✅ 左下角显示过滤率统计

---

## ⚠️ 常见问题

### 问题 1：前端无法连接后端

**症状**：前端显示"无法加载数据，请确认后端已启动"

**解决方案**：
1. 确认后端已启动（访问 http://localhost:8000）
2. 检查 `frontend/.env` 中的 `VITE_API_BASE_URL` 是否正确
3. 检查浏览器控制台是否有 CORS 错误

### 问题 2：后端启动失败（数据库连接错误）

**症状**：后端启动时报错 `Can't connect to MySQL server`

**解决方案**：
这是正常的，因为项目会尝试连接数据库。有两个选择：

**选项 A**：安装 MySQL（推荐，完整功能）
```bash
brew install mysql
brew services start mysql
mysql -u root -p < backend/database/init_db.sql
```

**选项 B**：忽略数据库错误（部分功能可用）
- 实时信息流和数据统计功能不依赖数据库，可以正常使用
- Agent 3 查询会使用内存数据，不会保存历史记录

### 问题 3：Agent 3 查询失败

**症状**：点击 Agent 3 查询时报错

**可能原因**：
1. Agent API 密钥无效或过期
2. 网络连接问题

**解决方案**：
检查 `backend/.env` 中的 `AGENT_API_KEY` 是否有效

### 问题 4：npm install 失败

**解决方案**：
```bash
# 清除缓存
npm cache clean --force

# 删除 node_modules 重新安装
rm -rf node_modules package-lock.json
npm install
```

---

## 📊 功能验证清单

启动成功后，验证以下功能：

### 基础功能（不需要数据库）
- [ ] Dashboard 页面加载
- [ ] 市场选择器切换（JP/KR/SEA/US）
- [ ] 实时信息流显示数据
- [ ] 左下角过滤率统计显示
- [ ] 时间旅行滑块可以拖动
- [ ] 待核验热点面板显示

### Agent 功能（需要 API 密钥）
- [ ] Agent 3 对话面板打开
- [ ] 发送问题并收到回复
- [ ] 人类经验注入功能

### 数据库功能（需要 MySQL）
- [ ] 对话历史保存
- [ ] 简报历史查询
- [ ] 删除对话记录

---

## 🎯 黑客松演示建议

如果时间紧迫，可以按以下优先级准备：

### 必须可用（P0）
1. ✅ 实时信息流展示
2. ✅ 双重门控去噪系统状态
3. ✅ 时间旅行功能
4. ✅ Agent 3 基本查询

### 建议可用（P1）
1. 待核验热点面板交互
2. 人类经验注入演示
3. 对话历史功能

### 可选（P2）
1. 简报生成
2. 营销工具按钮

---

## 📝 环境变量说明

### 前端 (`frontend/.env`)
```
VITE_API_BASE_URL=http://localhost:8000
```

### 后端 (`backend/.env`)
```
# 数据库（可选）
DATABASE_URL=mysql+pymysql://root:password@localhost:3306/compass_jewel

# Agent API（必需，用于 Agent 3 查询）
AGENT_API_KEY=your_api_key
AGENT_MODEL=claude-sonnet-4-6
AGENT_BASE_URL=https://www.findcg.com/v1
```

---

## 🔧 开发模式 vs 生产模式

### 开发模式（当前配置）
- 前端：`npm run dev` - 热重载，快速开发
- 后端：`python main.py` - 自动重载
- 数据库：可选

### 生产部署
参考 `docs/DEPLOYMENT.md` 进行部署配置

---

## 💡 提示

1. **首次启动建议**：先启动后端，确认数据加载成功，再启动前端
2. **数据刷新**：切换市场时会自动刷新数据
3. **API 文档**：访问 http://localhost:8000/docs 查看完整 API 文档
4. **日志查看**：后端终端会显示所有 API 请求日志

---

## 📞 需要帮助？

如果遇到问题：
1. 检查后端终端的错误日志
2. 检查浏览器控制台的错误信息
3. 确认所有依赖都已正确安装
4. 参考 `QUICKSTART.md` 和 `README.md`

---

祝黑客松顺利！🎉
